describe("spawnAsChild", () => {
	it.todo("lets to spawn an actor as the child of another");
	it.todo("returns the id of the spawned actor");
	it.todo("passes through spawn arguments");
	it.todo("is accessable by the parent");
});
